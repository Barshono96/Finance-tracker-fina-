import React from "react";
import SignUp from "@/components/auth/SignUp";

type Props = {}

const page = (props: Props) => {
  return (
    <div>
      <SignUp />
    </div>
  );
};

export default page;
