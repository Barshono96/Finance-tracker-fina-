import React from "react";
import TransactionList from "../../components/transaction/TransactionLists";
type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <TransactionList />
    </div>
  );
};

export default page;
